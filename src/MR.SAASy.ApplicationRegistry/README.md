# MR.SAASy.ApplicationRegistry

First platform implementation slice.

Planned responsibility:
- register and resolve applications by stable platform identity
- expose provider-neutral application/environment descriptors
- remain independent of Workslip domain models

Implementation starts only after the v0.1 contracts pass CI.
